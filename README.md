# DZ_12_holostih
DZ_12_holostih
# DHCP-Packet-Analyzer

Скрипт для анализа DHCP‑дампов (PCAPNG) с извлечением артефактов, визуализацией и детекцией подозрительной активности.

## Функционал

* Извлечение DHCP‑пакетов и DNS‑запросов из PCAPNG‑файла.
* Сохранение данных в CSV и JSON.
* Визуализация статистики: графики DNS‑запросов по времени и распределения типов DHCP.
* Детекция подозрительных доменов и IP‑адресов.

## Требования

* Python 3.8+
* Библиотеки:
  * `scapy`
  * `matplotlib`
  * `seaborn`
  * `pandas`

## Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone <URL-репозитория>
   cd DHCP-Packet-Analyzer
   Шаг 1. Подготовка окружения и установка зависимостей
Создание виртуального окружения (рекомендуется)
bash
# Создаём виртуальное окружение
python -m venv dhcp-analyzer-env

# Активируем окружение:
# Windows:
dhcp-analyzer-env\Scripts\activate
# Linux/macOS:
source dhcp-analyzer-env/bin/activate
Установка необходимых библиотек
bash
pip install scapy matplotlib seaborn pandas
Описание библиотек:

scapy — для чтения и анализа сетевых пакетов из PCAP‑файлов;

matplotlib и seaborn — для построения графиков;

pandas — для обработки и анализа данных в табличном виде.

Шаг 2. Подготовка входных данных
Поместите файл дампа сети с именем dhcp.pcapng в ту же директорию, где будет находиться скрипт.

Шаг 3. Запуск скрипта
Сохраните код скрипта в файл dhcp_analyzer.py.

Запустите скрипт:

bash
python dhcp_analyzer.py
Шаг 4. Ожидаемый вывод скрипта
При успешном выполнении скрипт выведет в консоль:

🚀 Запуск анализа DHCP-дампа...
✅ Загружено 1573 пакетов
✅ Найдено 842 DHCP‑пакетов и 731 DNS‑запросов.
📊 Начало визуализации результатов...
🗃️ Таблица DHCP‑пакетов сохранена как 'dhcp_packets_table.csv'
🗃️ Список подозрительных IP и доменов сохранён как 'suspicious_artifacts.csv'
💾 Полный дамп DHCP сохранён как 'dhcp_packets_full.json'
🖼️ График DNS‑запросов сохранён как 'dns_requests_per_hour.png'
🖼️ График распределения DHCP‑типов сохранён как 'dhcp_types_distribution.png'
Шаг 5. Результаты работы скрипта
Скрипт создаст следующие файлы:

dhcp_packets_table.csv — таблица с основными данными DHCP‑пакетов (время, IP‑адреса, тип сообщения);

suspicious_artifacts.csv — список подозрительных IP‑адресов и доменов (длинные имена, домены с цифрами);

dhcp_packets_full.json — полный дамп всех извлечённых DHCP‑пакетов в формате JSON;

dns_requests_per_hour.png — график количества DNS‑запросов по часам;

dhcp_types_distribution.png — круговая диаграмма распределения типов DHCP‑сообщений.
Установите зависимости:

bash
pip install -r requirements.txt
Поместите файл дампа с именем dhcp.pcapng в директорию проекта.

Запустите скрипт:

bash
python dhcp_analyzer.py
Выходные файлы
dhcp_packets_table.csv — основные данные DHCP‑пакетов.

suspicious_artifacts.csv — подозрительные IP и домены.

dhcp_packets_full.json — полный дамп DHCP‑пакетов.

dns_requests_per_hour.png — график DNS‑запросов по часам.

dhcp_types_distribution.png — распределение типов DHCP‑сообщений.

Критерии подозрительности
Длина домена > 50 символов.

Наличие цифр в имени домена.
римечания
Скрипт обрабатывает только DHCP и DNS‑запросы (не ответы).

Временные метки конвертируются в читаемый формат.

Для больших дампов может потребоваться оптимизация памяти.


